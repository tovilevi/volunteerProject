import { shift } from "./shift"
import { user } from "./user"

export class linkUserToShift{
  
    IdlinkUserToTypeOfVolunteering:number=0
    idVolunteerTime:shift=new shift()
    idUser:user=new user()
}